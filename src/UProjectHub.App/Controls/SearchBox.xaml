<UserControl x:Class="UProjectHub.App.Controls.SearchBox"
             x:Name="Root"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Height="{DynamicResource Metric.SearchControlHeight}">
    <Border Padding="{DynamicResource Thickness.SearchPadding}"
            Background="{DynamicResource Brush.Surface}"
            BorderBrush="{DynamicResource Brush.BorderSubtle}"
            BorderThickness="1"
            CornerRadius="{DynamicResource CornerRadius.Control}">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <TextBlock Margin="{DynamicResource Thickness.IconGap}"
                       VerticalAlignment="Center"
                       Foreground="{DynamicResource Brush.TextSecondary}"
                       Text="⌕" />

            <Grid Grid.Column="1">
                <TextBox x:Name="SearchInput"
                         Padding="{DynamicResource Thickness.SearchTextInset}"
                         VerticalContentAlignment="Center"
                         Background="{DynamicResource Brush.Transparent}"
                         Foreground="{DynamicResource Brush.TextPrimary}"
                         CaretBrush="{DynamicResource Brush.TextPrimary}"
                         BorderThickness="0"
                         Text="{Binding Text, ElementName=Root, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                <TextBlock x:Name="SearchPlaceholder"
                           Margin="{DynamicResource Thickness.SearchTextInset}"
                           IsHitTestVisible="False"
                           VerticalAlignment="Center"
                           Foreground="{DynamicResource Brush.TextSecondary}"
                           Text="{DynamicResource String.SearchPlaceholder}">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock">
                            <Setter Property="Visibility" Value="Collapsed" />
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding Text, ElementName=SearchInput}" Value="">
                                    <Setter Property="Visibility" Value="Visible" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>
            </Grid>

            <Button Grid.Column="2"
                    Margin="8,0,0,0"
                    Command="{Binding ClearCommand, ElementName=Root}"
                    Content="×">
                <Button.Style>
                    <Style TargetType="Button" BasedOn="{StaticResource Button.Icon}">
                        <Setter Property="Visibility" Value="Visible" />
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding Text, ElementName=SearchInput}" Value="">
                                <Setter Property="Visibility" Value="Collapsed" />
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>
        </Grid>
    </Border>
</UserControl>
